"""
Tests for losses/*.

TODO:
  - test that adj returns scalar
  - test that error() keys match LOSS_COMPONENTS for each variant
  - test that update_loss_weights returns positive weights that sum > 0
  - test that SA-weight gradient ascent increases weights on high-residual points
"""
